import { NbrEtoilesPipe } from './nbr-etoiles.pipe';

describe('NbrEtoilesPipe', () => {
  it('create an instance', () => {
    const pipe = new NbrEtoilesPipe();
    expect(pipe).toBeTruthy();
  });
});
