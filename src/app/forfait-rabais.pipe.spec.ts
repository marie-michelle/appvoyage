import { ForfaitRabaisPipe } from './forfait-rabais.pipe';

describe('ForfaitRabaisPipe', () => {
  it('create an instance', () => {
    const pipe = new ForfaitRabaisPipe();
    expect(pipe).toBeTruthy();
  });
});
